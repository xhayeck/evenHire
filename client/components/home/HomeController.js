//Controller for home
angular.module('evenhire.home', [])

  .controller('HomeController', ['$scope','$state', 'Applicant', function($scope, $state, Applicant) {
    //Add functions we will want to run on the home page
}]);
